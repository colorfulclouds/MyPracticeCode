package priorityqueue;

import java.util.*;

//优先队列   优先队列   优先队列

public class priorityqueue {

	public static void main(String[] args) {
/*
		Scanner in = new Scanner(System.in);
		myCompare comp = new myCompare();
		//创建job 队列  comp为比较方法
		PriorityQueue<job> priQue = new PriorityQueue<job>(20, comp);
		
		System.out.println("一次输入时间片    0表示结束");
		
		boolean goon = true;
		int time;
		//输入时间片
		for(int cnt = 1;goon;cnt++){
			
			System.out.print("作业"+cnt+"所需时间:");
			time = in.nextInt();
			
			if(time>0)
				priQue.add(new job(cnt,time));
			else
				goon = false;
			
		}
		
		System.out.println("调度顺序为:(升序)");
		while(priQue.size()>0){
			job tj;
			tj = priQue.poll();
			System.out.println("作业编号"+tj.getnumber()+"所需时间"+tj.getspend());
		}
		*/
	}
	

}
